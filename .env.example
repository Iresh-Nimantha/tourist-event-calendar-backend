# API Base URL
# For development (local backend)
VITE_API_URL=http://localhost:5000/api

# For production, set this to your deployed backend API URL
# Example: VITE_API_URL=https://api.yourdomain.com/api
# Make sure to include /api at the end if your backend routes are prefixed with /api
